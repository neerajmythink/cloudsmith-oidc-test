# cloudsmith-oidc-test
OpenID Connect (OIDC) with Cloudsmith is moving away from long-lived API keys in favor of short-lived, secure tokens. This is a "Zero Trust" approach where GitHub proves its identity to Cloudsmith for every run.

